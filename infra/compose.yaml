services:
  database:
    image: "postgres:16.13-alpine3.23"
    environment:
      # POSTGRES_USER: "tabnews"
      POSTGRES_PASSWORD: "local_password"
      # POSTGRES_DB: "tabnews"
    ports:
      - "5432:5432"
